# 设计计划卡 · AI-SSH 前端

> 依据 `frontend-design-agent` 原创设计引擎（design-synthesis.md）推导，落地前存档备查。

## 项目类型
桌面运维工具（Tauri 应用）/ 暗色默认；锚点体系：terminal（仅工程校准：动效节奏、对比度处理，不搬入色板骨架）。

## 原创画面句
**深夜机房的示波器台架**——琥珀示波线在深炭屏上持续扫描，SSH 通道以等宽行流输送命令与指标，AI 以逐行打印的方式回应诊断；危险动作在屏上打出琥珀硬警告。

## 矿脉（Step 1）
| 采矿点 | 素材 |
|---|---|
| 器物/材质 | 示波器、CRT 荧光屏、终端窗口、命令提示符、SSH 管道、审计日志 |
| 情绪/文化 | 深夜值班机房、连接会话的仪式感、逐行打印的节奏 |
| 空间/时间 | 机房深夜；命令执行的"时刻"；会话时间线 |
| 行话/语汇 | prompt、pty、channel、exit code、uptime、loadavg、iowait、Top10、Traceback、回滚、脱敏 |
| 张力/冲突 | 危险命令 vs 安全护栏；AI 自动 vs 人工确认；云端 LLM vs 纯本地 |

## 五维合成（Step 3，每维"为什么来自项目"）
| 维度 | 推导 | 落地 |
|---|---|---|
| 色彩 | 示波器琥珀线 → 主色相 36°；深炭屏 → 60% 深墨暖炭；互补电光蓝 → 10% 撞色焦点；青绿 → AI 签名色 | 见 tokens.css |
| 排版 | 终端台架：左主机面板 + 中央终端 + 右 AI 分屏 + 底部示波条带；等宽字体为骨架 | --font-body=等宽族 |
| 形状 | CRT/终端像素感：小圆角(2/4/8)、发丝线分隔、块状光标 | --radius-* 覆盖 |
| 动效 | 逐行打印（AI 流式）、扫描线掠过（监控刷新）、光标闪烁、连接握手脉冲 | --t-fast/normal/slow 快节奏 |
| 图标 | 终端窗、示波波形、管道、盾、记忆卡、信号灯、回滚 | 自绘 SVG（app/icons/） |

## 签名元素（Step 4）
**块状闪烁光标 + 示波扫描线**：AI 输出以"逐行打印"渲染（等宽 + 块光标），监控面板刷新时一条青绿扫描线掠过；危险命令确认时整卡打上琥珀警告边框。换一个 brief 即失效。

## 惯例打破（Step 5 / Anti-template）
- 这类 AI 运维工具通用模板 = 紫蓝玻璃拟态 + 光晕科技感 → 本设计偏离为 CRT 终端硬核质感：无玻璃拟态、无大渐变光晕、无 Inter 字体，等宽为主、发丝线分隔、撞色焦点仅电光蓝一处。
- 深色科技感反面教材（visual-language 四类）全部避开。

## 60-30-10 撞色
- 60% 基底：#141210 深炭暖墨（带琥珀色相，非纯黑）
- 30% 次要：#26211B/#1F1B16 同色相分层 + 发丝线 #3B332A
- 10% 主强调 --acc：#4D8DFF 电光蓝（执行/连接/主 CTA，唯一大面积焦点）；--acc2 青绿（AI 签名/流式光标）；--acc3 紫（hover 微点缀）
- 语义色：ok #2FC66E / warn #F0A64A（示波线色）/ err #E5484D
- 流光/渐变：仅 AI 输入框 focus-within 一处（核心交互），其余安静

## 动效四层
| 层 | 方案 |
|---|---|
| L1 加载/入场 | 连接时"握手脉冲"（命令提示符闪烁）+ 面板淡入，内容先可见 |
| L2 悬停/微交互 | 终端反色（hover 行反色）、按钮上浮 1px + 增亮，0.15s |
| L3 滚动叙事 | 无长页滚动叙事（应用型），会话日志随流增长 |
| L4 环境氛围 | 监控面板青绿扫描线掠过（2.4s linear 循环，低透明度）|

## 排版/细节要点（design-thinking 层）
- 主角：中央终端（用户 80% 时间在这）；AI 面板是"副驾"；监控是"仪表"。
- 动线：主机列表（选）→ 终端（操作）→ AI 确认卡（决策）→ 监控（回看）。
- 节奏：终端密、面板中、监控卡稀疏；巨幅数字（display）只在监控概览出现 1 处。
- 状态矩阵：按钮/输入/卡片全状态；危险确认弹窗 = 红色警告 + 输入主机名（PRD F6.1）。
- 文案：运维行话（"连接中…""exit 0""已回滚"），不用 AI 高频词。

## 组件清单（工程层）
Button（primary/ghost/danger）、Tag（ok/warn/err/info/neutral）、Card（主机卡/监控卡/告警卡）、Field（表单）、Toast、Modal（危险确认/主机编辑）、Tabs、EmptyState、Icon（自绘 SVG sprite）。
