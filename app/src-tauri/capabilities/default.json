{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AI-SSH 默认能力：核心 + 对话框（导出 CSV / 导入 MD 用）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default"
  ]
}
