[workspace]
resolver = "2"
# app/src-tauri 为独立 crate（Tauri 应用各自维护 lockfile），不纳入 workspace
members = ["crates/ai-ssh-core"]

[workspace.package]
version = "1.2.0"
edition = "2021"
license = "MIT"

[profile.release]
lto = "thin"
opt-level = 2
